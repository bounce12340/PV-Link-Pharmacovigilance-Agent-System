# PV-Link 前端環境變數範例。複製為 .env.local 後填入。
# 注意：VITE_ 前綴的變數會被打包進前端，僅適合本機開發；
# 公開/多人部署請改用後端 proxy（見下方 A 方案），前端不放金鑰。

# ── A. 公開/多人部署（建議）：走後端 proxy + Cloudflare Access，前端不含金鑰 ──
# 同源部署（前端與 Worker 同網域）下設為相對路徑即可；正式建置已寫在 .env.production。
# 存取控制交給 Cloudflare Access（登入後 cookie 自動帶上），前端不需任何密鑰。
# VITE_PV_PROXY_ENDPOINT=/api

# ── B. 本機開發：前端直連任一 OpenAI 相容端點 ──
# OpenAI 官方
VITE_LLM_BASE_URL=https://api.openai.com/v1
VITE_LLM_API_KEY=sk-xxxx
VITE_LLM_MODEL=gpt-4o-mini
# OpenRouter 範例：
#   VITE_LLM_BASE_URL=https://openrouter.ai/api/v1
#   VITE_LLM_MODEL=moonshotai/kimi-k2
# 本機 Ollama 範例：
#   VITE_LLM_BASE_URL=http://localhost:11434/v1
#   VITE_LLM_API_KEY=ollama
#   VITE_LLM_MODEL=llama3.1
# 若上游不支援 response_format（部分 Ollama/OpenRouter 模型），關閉 JSON 模式：
#   VITE_LLM_JSON_MODE=0

# ── NCBI PubMed（選填）：設定後速率上限由 3 req/s 放寬到 10 req/s ──
# VITE_NCBI_API_KEY=your_ncbi_key

# ── 不良反應（AE）個案通報端點（選填）──────────────────────────
# 未設定時，業務端手機通報會直接寫入「同一個瀏覽器」的 IndexedDB，
# 只適合單機試用與展示；要讓業務在外面用手機通報、藥安人員在辦公室收案，
# 必須設定一個真正的後端端點（接收 POST JSON 的 AEReport）。
# VITE_AE_API_ENDPOINT=https://your-pv-backend.example.com/api/ae-reports
# 與後端共享的簡易存取權杖（防開放式代理；非機密等級憑證，真正的認證請用 Cloudflare Access 等機制）
# VITE_AE_API_TOKEN=change-me
