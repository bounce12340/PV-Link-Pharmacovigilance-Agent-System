# 雙語切換與明暗主題 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 為 PV-Link 前端加入中英雙語切換與明暗主題切換，並將 Tailwind 從 CDN 正規化為建置依賴。

**Architecture:** 自製輕量 i18n（字典 + React Context + `useT`）；主題以 Tailwind `dark` class 策略 + `ThemeProvider` 控制；AI 產出函式加 `lang` 參數依語言產出。偏好記憶於 localStorage。

**Tech Stack:** React 19、TypeScript 5.8、Vite 6、Tailwind CSS（改為 PostCSS 建置）、Vitest。

## Global Constraints

- 不加任何 runtime 依賴；i18n 全自製。devDependencies 僅新增 `tailwindcss`、`postcss`、`autoprefixer`。
- `PVRecord` 型別與欄位名（`summary_zh`、`conclusion_zh`）不變——AI 產英文時仍寫入這些欄位。
- localStorage keys：語言 `PV_LANG`（值 `zh`/`en`，預設 `zh`）、主題 `PV_THEME`（值 `light`/`dark`，預設 `light`）。
- 專案 `package.json` 為 `"type": "module"`：所有設定檔用 ESM（`export default`）。
- 每個 Task 結束前必過：`npm run typecheck`、`npm test`（既有 23 測試維持綠）、`npm run build`。樣式相關 Task 額外需 `npm run preview` 目視。
- 使用者慣例：每個 Task 各自 commit 並直接 push origin main。

---

### Task 1: Tailwind 正規化（CDN → PostCSS 建置）

**Files:**
- Create: `tailwind.config.js`
- Create: `postcss.config.js`
- Create: `index.css`
- Modify: `index.html`（移除 CDN Tailwind script 與 inline `<style>`；移除 `<link rel="stylesheet" href="/index.css">`）
- Modify: `index.tsx`（頂部加 `import './index.css';`）
- Modify: `package.json`（devDependencies 加三個套件）

**Interfaces:**
- Produces: 全域可用的 Tailwind utility class（含 `dark:` variant，class 策略）。後續 Task 2/3 依賴 `dark` class 生效。

- [ ] **Step 1: 安裝建置依賴**

Run:
```bash
npm install -D tailwindcss@^3 postcss@^8 autoprefixer@^10
```
Expected: 三個套件寫入 `package.json` 的 devDependencies，無錯誤。

- [ ] **Step 2: 建立 `tailwind.config.js`**

```js
/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class',
  content: ['./index.html', './index.tsx', './App.tsx', './services/**/*.{ts,tsx}', './i18n/**/*.{ts,tsx}', './theme/**/*.{ts,tsx}'],
  theme: { extend: {} },
  plugins: [],
};
```

- [ ] **Step 3: 建立 `postcss.config.js`**

```js
export default { plugins: { tailwindcss: {}, autoprefixer: {} } };
```

- [ ] **Step 4: 建立 `index.css`**（把原 index.html 的 body 字型一併移進來）

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

body { font-family: 'Inter', 'Noto Sans TC', sans-serif; }
```

- [ ] **Step 5: 改 `index.html`**

移除這三段：`<script src="https://cdn.tailwindcss.com"></script>`、`<style> body { ... } </style>`、`<link rel="stylesheet" href="/index.css">`。
保留：Google Fonts 的 preconnect/link、importmap（不動）、`<div id="root">`、`<script type="module" src="/index.tsx">`。

- [ ] **Step 6: 改 `index.tsx`** — 第一行 import 之上加：

```tsx
import './index.css';
```

- [ ] **Step 7: 驗證 build 與樣式**

Run: `npm run typecheck && npm run build`
Expected: build 成功，`dist/assets/*.css` 有產出（先前 CDN 版沒有 CSS 產出，這是正規化生效的證據）。

Run: `npm run preview`（背景啟動後開 http://localhost:4173 目視）
Expected: 版面與正規化前一致——水彩漸層背景、玻璃卡片、字型都在。若某些任意值 class（`bg-[#f8fafc]`、`blur-[120px]`、`animate-[pulse_8s_infinite]`）未生效，確認 content glob 有涵蓋該檔。

- [ ] **Step 8: Commit**

```bash
git add tailwind.config.js postcss.config.js index.css index.html index.tsx package.json package-lock.json
git commit -m "build: Tailwind 由 CDN 正規化為 PostCSS 建置依賴"
git push origin main
```

---

### Task 2: ThemeProvider 與主題切換鈕

**Files:**
- Create: `theme/ThemeContext.tsx`
- Create: `tests/theme.test.ts`
- Modify: `index.tsx`（以 `<ThemeProvider>` 包住 `<App/>`）
- Modify: `App.tsx`（header 加 ☀/🌙 切換鈕）

**Interfaces:**
- Produces: `ThemeProvider`（React 元件）、`useTheme(): { theme: 'light'|'dark'; toggle: () => void }`。Task 3 的 `dark:` 樣式依賴此 provider 對 `<html>` toggle `dark` class。

- [ ] **Step 1: 寫失敗測試 `tests/theme.test.ts`**

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { readInitialTheme } from '../theme/ThemeContext';

describe('theme', () => {
  beforeEach(() => localStorage.clear());
  it('defaults to light when unset', () => {
    expect(readInitialTheme()).toBe('light');
  });
  it('reads persisted value', () => {
    localStorage.setItem('PV_THEME', 'dark');
    expect(readInitialTheme()).toBe('dark');
  });
  it('ignores invalid value', () => {
    localStorage.setItem('PV_THEME', 'purple');
    expect(readInitialTheme()).toBe('light');
  });
});
```

- [ ] **Step 2: 執行確認失敗**

Run: `npx vitest run tests/theme.test.ts`
Expected: FAIL（`readInitialTheme` 尚未定義／模組不存在）。

- [ ] **Step 3: 實作 `theme/ThemeContext.tsx`**

```tsx
import React, { createContext, useContext, useEffect, useState } from 'react';

export type Theme = 'light' | 'dark';
const KEY = 'PV_THEME';

export function readInitialTheme(): Theme {
  try {
    const s = localStorage.getItem(KEY);
    if (s === 'dark' || s === 'light') return s;
  } catch { /* ignore */ }
  return 'light';
}

const ThemeContext = createContext<{ theme: Theme; toggle: () => void }>({ theme: 'light', toggle: () => {} });

export const ThemeProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  const [theme, setTheme] = useState<Theme>(readInitialTheme);
  useEffect(() => {
    const root = document.documentElement;
    if (theme === 'dark') root.classList.add('dark'); else root.classList.remove('dark');
    try { localStorage.setItem(KEY, theme); } catch { /* ignore */ }
  }, [theme]);
  const toggle = () => setTheme(t => (t === 'dark' ? 'light' : 'dark'));
  return <ThemeContext.Provider value={{ theme, toggle }}>{children}</ThemeContext.Provider>;
};

export const useTheme = () => useContext(ThemeContext);
```

- [ ] **Step 4: 執行確認通過**

Run: `npx vitest run tests/theme.test.ts`
Expected: PASS（3 個測試）。

- [ ] **Step 5: 在 `index.tsx` 包 provider**

把 `<App />` 包起來（ThemeProvider 在最外，Task 4 的 LangProvider 之後補在內層）：
```tsx
import { ThemeProvider } from './theme/ThemeContext';
// ...
root.render(
  <React.StrictMode>
    <ThemeProvider>
      <App />
    </ThemeProvider>
  </React.StrictMode>
);
```

- [ ] **Step 6: header 加切換鈕**

在 `App.tsx` header（`<h1>PV-Link Auditor</h1>` 所在的 `<header>`）右側、「啟動新監測任務」按鈕之前，插入主題切換鈕。先在 `App` 元件內取用 hook：`const { theme, toggle } = useTheme();`（import `useTheme`）。按鈕：
```tsx
<button onClick={toggle} title="切換主題" className="p-2.5 rounded-2xl bg-white/40 dark:bg-white/10 text-slate-700 dark:text-slate-200 border border-white/40 dark:border-white/10 hover:bg-white/60 dark:hover:bg-white/20 transition-all">
  {theme === 'dark' ? '☀️' : '🌙'}
</button>
```

- [ ] **Step 7: 驗證**

Run: `npm run typecheck && npm test && npm run build`
Expected: typecheck 乾淨；測試 23 + 3 = 26 綠；build 成功。
`npm run preview` 目視：點 🌙/☀️ 鈕會 toggle `<html class="dark">`（開發者工具可見），偏好重整後保留。此時暗色樣式尚未套（Task 3），只需確認 class 有 toggle。

- [ ] **Step 8: Commit**

```bash
git add theme/ThemeContext.tsx tests/theme.test.ts index.tsx App.tsx
git commit -m "feat: 加入 ThemeProvider 與明暗切換鈕"
git push origin main
```

---

### Task 3: 暗色 dark: 變體覆蓋

**Files:**
- Modify: `App.tsx`（全檔各容器加 `dark:` 變體）

**Interfaces:**
- Consumes: Task 2 的 `dark` class（由 `ThemeProvider` toggle 於 `<html>`）。
- Produces: 完整暗色版視覺。

這是機械但大量的工作。**沒有單元測試可驗，靠 typecheck/build + preview 目視。** 依下表逐一掃描 `App.tsx` 並加對應 `dark:` 變體（原 class 保留，補上 `dark:`）：

| 原 class（亮色） | 補上的 dark: 變體 |
|---|---|
| `bg-[#f8fafc]`（水彩底層 fixed div） | `dark:bg-[#0b1020]` |
| `bg-indigo-200/40`、`bg-rose-200/40`、`bg-teal-200/40`、`bg-amber-100/60`（漸層 blob） | 各加 `dark:bg-indigo-500/20`、`dark:bg-rose-500/15`、`dark:bg-teal-500/15`、`dark:bg-amber-500/10` |
| `bg-white/30`、`bg-white/40`、`bg-white/50`、`bg-white/60` | 對應 `dark:bg-white/5`、`dark:bg-white/[0.07]`、`dark:bg-white/[0.08]`、`dark:bg-white/10` |
| `bg-white/80`（輸入框） | `dark:bg-slate-800/80` |
| `border-white/40`、`border-white/60`、`border-white/80` | 對應 `dark:border-white/10`、`dark:border-white/10`、`dark:border-white/15` |
| `text-slate-900`、`text-slate-800` | `dark:text-slate-100` |
| `text-slate-700` | `dark:text-slate-200` |
| `text-slate-600`、`text-slate-500`、`text-slate-400` | `dark:text-slate-400` |
| `border-slate-300`、`border-slate-200/60` | `dark:border-slate-600`、`dark:border-slate-700/60` |
| `bg-slate-900/80`（頂欄）、`bg-slate-950/85`（日誌） | 維持（本就深色，僅確認文字對比 OK） |
| 分頁選中 `bg-indigo-600/90`、`bg-emerald-600/90`、`bg-rose-600/90` | 維持（品牌色在暗色下仍可用） |

代表性範例（頂部水彩層改後）：
```tsx
<div className="fixed inset-0 -z-10 bg-[#f8fafc] dark:bg-[#0b1020]">
   <div className="absolute top-[-10%] left-[-10%] w-[50%] h-[50%] bg-indigo-200/40 dark:bg-indigo-500/20 rounded-full blur-[120px] mix-blend-multiply dark:mix-blend-screen animate-[pulse_8s_infinite]" />
   {/* 其餘三個 blob 同理，並把 mix-blend-multiply 加上 dark:mix-blend-screen */}
</div>
```
注意：`mix-blend-multiply` 在暗底會讓 blob 消失，暗色需改 `dark:mix-blend-screen`——所有四個 blob 都加。

- [ ] **Step 1: 逐區塊套用 dark: 變體**

依序處理：水彩背景層 → 頂欄 → header → 側邊分頁 → 檢索設定頁 → 待核閱清單與詳情卡 → 結構化 PV 數據卡 → 正式庫表格 → 訊號聚合頁 → 日誌頁。每處理完一個大區塊，`npm run build` 一次確保沒打壞 JSX。

- [ ] **Step 2: typecheck 與 build**

Run: `npm run typecheck && npm run build`
Expected: 乾淨、成功。

- [ ] **Step 3: preview 目視（明暗兩態）**

Run: `npm run preview`
Expected（暗色）：背景深色霓虹、卡片深色半透明、文字亮色可讀、無「亮底黑字」或「暗底暗字」的死角。逐頁切換檢查五個分頁。

- [ ] **Step 4: Commit**

```bash
git add App.tsx
git commit -m "feat: 暗色主題 dark: 變體（保留水彩設計語言）"
git push origin main
```

---

### Task 4: i18n 基礎（translations + LangContext + 語言切換鈕）

**Files:**
- Create: `i18n/translations.ts`
- Create: `i18n/LangContext.tsx`
- Create: `tests/i18n.test.ts`
- Modify: `index.tsx`（在 ThemeProvider 內包 LangProvider）
- Modify: `App.tsx`（header 加語言切換鈕；此 Task 僅建立機制，字串 key 化在 Task 5）

**Interfaces:**
- Produces：
  - `type Lang = 'zh' | 'en'`
  - `type TransKey`（= `keyof typeof translations['zh']`）
  - `translations: Record<Lang, Record<TransKey, string>>`
  - `LangProvider`（元件）、`useLang(): { lang: Lang; setLang: (l: Lang) => void; t: (k: TransKey) => string }`、`useT(): (k: TransKey) => string`
- Task 5、6 消費 `useT`、`useLang`。

- [ ] **Step 1: 寫失敗測試 `tests/i18n.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { translations } from '../i18n/translations';

describe('i18n translations', () => {
  it('zh and en have identical key sets', () => {
    const zh = Object.keys(translations.zh).sort();
    const en = Object.keys(translations.en).sort();
    expect(en).toEqual(zh);
  });
  it('no empty values in either language', () => {
    (['zh', 'en'] as const).forEach(lang => {
      Object.entries(translations[lang]).forEach(([k, v]) => {
        expect(v, `${lang}.${k} is empty`).toBeTruthy();
      });
    });
  });
});
```

- [ ] **Step 2: 執行確認失敗**

Run: `npx vitest run tests/i18n.test.ts`
Expected: FAIL（`i18n/translations` 不存在）。

- [ ] **Step 3: 建 `i18n/translations.ts`（先放少量種子 key，Task 5 再補齊）**

```ts
export type Lang = 'zh' | 'en';

const zh = {
  'nav.input': '檢索設定',
  'nav.review': '待核閱',
  'nav.database': '正式庫',
  'nav.signals': '訊號聚合',
  'nav.logs': '系統日誌',
  'header.subtitle': '專業稽核模式 (PRO-V3)',
  'header.run': '啟動新監測任務',
} as const;

export type TransKey = keyof typeof zh;

const en: Record<TransKey, string> = {
  'nav.input': 'Search',
  'nav.review': 'Review',
  'nav.database': 'Database',
  'nav.signals': 'Signals',
  'nav.logs': 'Logs',
  'header.subtitle': 'Professional Audit Mode (PRO-V3)',
  'header.run': 'Start New Monitoring Run',
};

export const translations: Record<Lang, Record<TransKey, string>> = { zh, en };
```
（`en` 標註 `Record<TransKey, string>` → 漏 key 會 typecheck 失敗，與測試雙保險。）

- [ ] **Step 4: 建 `i18n/LangContext.tsx`**

```tsx
import React, { createContext, useContext, useEffect, useState } from 'react';
import { translations, TransKey, Lang } from './translations';

const KEY = 'PV_LANG';

export function readInitialLang(): Lang {
  try {
    const s = localStorage.getItem(KEY);
    if (s === 'zh' || s === 'en') return s;
  } catch { /* ignore */ }
  return 'zh';
}

const LangContext = createContext<{ lang: Lang; setLang: (l: Lang) => void; t: (k: TransKey) => string }>({
  lang: 'zh', setLang: () => {}, t: (k) => k,
});

export const LangProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  const [lang, setLang] = useState<Lang>(readInitialLang);
  useEffect(() => { try { localStorage.setItem(KEY, lang); } catch { /* ignore */ } }, [lang]);
  const t = (k: TransKey) => translations[lang][k] ?? translations.zh[k] ?? k;
  return <LangContext.Provider value={{ lang, setLang, t }}>{children}</LangContext.Provider>;
};

export const useLang = () => useContext(LangContext);
export const useT = () => useContext(LangContext).t;
```

- [ ] **Step 5: 執行確認測試通過**

Run: `npx vitest run tests/i18n.test.ts`
Expected: PASS（2 個測試）。

- [ ] **Step 6: `index.tsx` 包 LangProvider（在 ThemeProvider 內）**

```tsx
import { LangProvider } from './i18n/LangContext';
// ...
<ThemeProvider>
  <LangProvider>
    <App />
  </LangProvider>
</ThemeProvider>
```

- [ ] **Step 7: header 加語言切換鈕**

`App` 內：`const { lang, setLang } = useLang();`。在主題鈕旁加：
```tsx
<div className="flex rounded-2xl overflow-hidden border border-white/40 dark:border-white/10 text-[11px] font-black">
  <button onClick={() => setLang('zh')} className={`px-3 py-2 transition-all ${lang === 'zh' ? 'bg-indigo-600/90 text-white' : 'bg-white/40 dark:bg-white/10 text-slate-600 dark:text-slate-300'}`}>中</button>
  <button onClick={() => setLang('en')} className={`px-3 py-2 transition-all ${lang === 'en' ? 'bg-indigo-600/90 text-white' : 'bg-white/40 dark:bg-white/10 text-slate-600 dark:text-slate-300'}`}>EN</button>
</div>
```

- [ ] **Step 8: 驗證與 Commit**

Run: `npm run typecheck && npm test && npm run build`
Expected: typecheck 乾淨；測試 26 + 2 = 28 綠；build 成功。
```bash
git add i18n/ tests/i18n.test.ts index.tsx App.tsx
git commit -m "feat: i18n 機制（translations + LangProvider + 語言切換鈕）"
git push origin main
```

---

### Task 5: UI 字串 key 化

**Files:**
- Modify: `App.tsx`（所有寫死繁中 UI 字串換 `t('key')`）
- Modify: `i18n/translations.ts`（補齊所有 key 的 zh/en）

**Interfaces:**
- Consumes: Task 4 的 `useT`、`TransKey`。

方法：掃描 `App.tsx` 所有面向使用者的繁中字串（JSX 文字節點、`placeholder`、`title`、`alert()`/`window.confirm()` 訊息、CSV headers 保持英文不動、log 訊息可留繁中不 key 化——log 屬內部技術輸出）。每個字串在 `translations.ts` 新增一組 `zh`/`en`，JSX 改用 `{t('key')}`。

命名規則：`區塊.語意`。區塊用 `nav`/`header`/`input`/`review`/`db`/`signals`/`common`。範例對照：

| 位置 | key | zh | en |
|---|---|---|---|
| 檢索設定標題 | `input.title` | `藥物監測配置` | `Monitoring Configuration` |
| 成分欄 label | `input.ingredients` | `目標成分 (必填，多成分請用逗號分隔)` | `Active Ingredients (required, comma-separated)` |
| 監測起始 | `input.dateFrom` | `監測起始` | `From` |
| 監測結束 | `input.dateTo` | `監測結束` | `To` |
| AE 關鍵字 label | `input.aeTerms` | `AE 關鍵字 (逗號分隔，支援 * 萬用字元)` | `AE Terms (comma-separated, * wildcard)` |
| 排除詞 label | `input.exclusions` | `排除詞 (逗號分隔，選填，以 NOT 排除)` | `Exclusions (comma-separated, optional)` |
| 待核閱空狀態 | `review.empty` | `無待核閱文獻` | `No literature to review` |
| 相關性門檻 | `review.threshold` | `相關性` | `Relevance` |
| 臨床結論卡標題 | `review.conclusion` | `臨床結論 (Key Conclusion)` | `Key Conclusion` |
| AI 摘要卡標題 | `review.summary` | `AI 完整摘要 (Summary)` | `AI Summary` |
| 結構化數據卡標題 | `review.structured` | `結構化 PV 數據 (Structured Extraction)` | `Structured PV Data` |
| 匯入按鈕 | `review.import` | `確認匯入正式庫` | `Import to Database` |
| 複製 | `common.copy` | `複製` | `Copy` |
| 已複製 | `common.copied` | `已複製!` | `Copied!` |
| 正式庫標題 | `db.title` | `正式文獻庫` | `Master Literature Database` |
| 匯出篩選 | `db.exportFiltered` | `匯出篩選結果` | `Export Filtered` |
| 匯出全庫 | `db.exportAll` | `匯出全庫` | `Export All` |
| 全域檢索 placeholder | `db.searchPlaceholder` | `全域檢索: PMID、標題、成分或摘要關鍵字...` | `Search: PMID, title, ingredient, abstract...` |
| 查無資料 | `db.empty` | `查無資料` | `No records` |
| 訊號聚合標題 | `signals.title` | `安全訊號聚合` | `Safety Signal Aggregation` |
| 刪除確認 | `db.deleteConfirm` | `確定要刪除此文獻嗎？此動作將永久移除這筆紀錄。` | `Delete this record permanently?` |

（以上為代表；實作時逐一補齊 `App.tsx` 出現的每個面向使用者字串，估計總計 80–120 個 key。結構化 PV 數據表格的欄位 label 如「成分 (Ingredient)」等亦 key 化。）

- [ ] **Step 1: 逐區塊 key 化**

從 `nav`（側邊分頁）開始，依序 header → input → review → db → signals。每 key 化一個區塊，就在 `translations.ts` 補對應 zh/en，`npm run typecheck` 確認 `TransKey` 有涵蓋（用到未定義 key 會 typecheck 失敗，這是你的即時保護網）。

- [ ] **Step 2: i18n key 一致性測試維持綠**

Run: `npx vitest run tests/i18n.test.ts`
Expected: PASS——若 en 漏補某 key，此測試與 typecheck 會同時抓到。

- [ ] **Step 3: 全量驗證**

Run: `npm run typecheck && npm test && npm run build`
Expected: 全綠、成功。

- [ ] **Step 4: preview 目視雙語**

Run: `npm run preview`
Expected: 點「中 / EN」即時切換所有 UI 文字；切到 EN 無殘留繁中（AI 已產出的摘要/結論除外——那屬 Task 6 範疇）。

- [ ] **Step 5: Commit**

```bash
git add App.tsx i18n/translations.ts
git commit -m "feat: UI 字串全面 key 化（中英）"
git push origin main
```

---

### Task 6: AI 產出加 lang 參數與「以當前語言重新產出」

**Files:**
- Modify: `services/llmService.ts`（`scoreRelevance`、`generateSummaries`、`extractPVData` 加 `lang` 參數）
- Modify: `App.tsx`（呼叫處傳 `lang`；詳情頁加重新產出按鈕）
- Modify: `i18n/translations.ts`（重新產出按鈕文案）

**Interfaces:**
- Consumes: Task 4 的 `useLang`（取 `lang`）。
- 變更簽名（新增選填參數，向後相容）：
  - `scoreRelevance(records, lang?: 'zh'|'en', onProgress?)`
  - `generateSummaries(records, lang?: 'zh'|'en', onProgress?)`
  - `extractPVData(record, lang?: 'zh'|'en')`

- [ ] **Step 1: `llmService.ts` 加語言化 prompt**

在檔案頂部加語言指示常數：
```ts
const langDirective = (lang: 'zh' | 'en') =>
  lang === 'en'
    ? 'Respond in English. summary and conclusion fields must be in English.'
    : '請以繁體中文回答。summary 與 conclusion 欄位必須是繁體中文。';
```
`generateSummaries(records, lang = 'zh', onProgress?)`：把 `langDirective(lang)` 併入 prompt（既有繁中指示改為由 `lang` 決定）。`extractPVData(record, lang = 'zh')` 同理在 prompt 尾端加 `langDirective(lang)`。`scoreRelevance` 的 `reason` 欄位亦依 `lang`。**欄位名 `summary_zh`/`conclusion_zh` 不變。**

- [ ] **Step 2: `App.tsx` 呼叫處傳 lang**

`runWorkflow` 內：`const lang = ...`（用 `useLang().lang`，於元件頂層取得）。把 `llm.scoreRelevance(freshRecords)` → `llm.scoreRelevance(freshRecords, lang)`；`llm.generateSummaries(freshRecords)` → `llm.generateSummaries(freshRecords, lang)`。自動抽取的 `llm.extractPVData(selectedRecord)` → `llm.extractPVData(selectedRecord, lang)`。批次抽取 `extractPVDataBatch` 若有呼叫亦傳 lang（其內部對 `extractPVData` 傳遞）。

- [ ] **Step 3: 詳情頁「以當前語言重新產出」按鈕**

在結構化 PV 數據卡片標題列右側加按鈕，文案用 `t('review.regenerate')`（zh:`以當前語言重新產出` / en:`Regenerate in current language`）。onClick：
```tsx
const handleRegenerate = async (rec: PVRecord) => {
  const [sum] = await llm.generateSummaries([rec], lang);
  const pv = await llm.extractPVData(rec, lang);
  const upd = (r: PVRecord) => r.id !== rec.id ? r : { ...r, summary_zh: sum?.summary_zh ?? r.summary_zh, conclusion_zh: sum?.conclusion_zh ?? r.conclusion_zh, pv_data: { ...pv, ingredient: pv.ingredient && pv.ingredient !== 'N/A' ? pv.ingredient : r.original_search_term } };
  setRecords(prev => prev.map(upd));
  setMasterDatabase(prev => prev.map(upd));
};
```
（沿用既有 `ingredient` 不覆蓋原始搜尋詞的邏輯。）

- [ ] **Step 4: translations 補按鈕文案**

`review.regenerate`：zh `以當前語言重新產出`、en `Regenerate in current language`。

- [ ] **Step 5: 驗證**

Run: `npm run typecheck && npm test && npm run build`
Expected: 既有 28 測試維持綠（llmService 新參數為選填，不破壞既有測試）；typecheck 乾淨；build 成功。

- [ ] **Step 6: preview 端對端目視**

Run: `npm run preview`
Expected: 切 EN → 跑一次監測 → AI 摘要/結論為英文；對舊繁中記錄點「Regenerate」→ 該筆變英文。切回中文跑監測 → 繁中。

- [ ] **Step 7: Commit**

```bash
git add services/llmService.ts App.tsx i18n/translations.ts
git commit -m "feat: AI 產出依當前語言，詳情頁加重新產出按鈕"
git push origin main
```

---

## 部署驗證（全部完成後）

push main 會觸發 Pages 重建。等 deploy success 後於 https://pvlink.uic-ai.com 硬重整（Ctrl+F5），登入後端對端測：中英切換、明暗切換、偏好重整後保留、AI 依語言產出、重新產出按鈕。

## 完成準則

- `npm run typecheck` 乾淨、`npm test` 全綠（28+）、`npm run build` 成功、`npm run preview` 明暗×中英四種組合目視無破損。
- pvlink.uic-ai.com 部署後端對端可用。
