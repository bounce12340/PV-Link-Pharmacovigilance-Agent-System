# 正式建置（pvlink.uic-ai.com 同源部署）
# proxy 走同源相對路徑；存取控制由 Cloudflare Access 負責，前端不含任何金鑰或 token。
VITE_PV_PROXY_ENDPOINT=/api

# AE 個案收案 API（同源，走同一個 Worker、同一套 Access 驗證）。
# 設了這一行，業務手機送出的個案才會離開手機、進到 D1，後台才看得到。
VITE_AE_API_ENDPOINT=/api/ae-reports
